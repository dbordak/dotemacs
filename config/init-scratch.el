;; I'm not very Lispy.
(setq initial-major-mode 'text-mode)

;; Should hijack scratch buffer, doesn't seem to.
(setq remember-notes-buffer-name "*scratch*")

(setq initial-buffer-choice 'remember-notes)

(provide 'init-scratch)
